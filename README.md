这是一个 前端实现的 自托管wiki 页面，编辑页面由富文本实现，有Wiki的树状结构，代码部分全部由Codex 生成，目前基本功能实现，bug需要一步一调试，欢迎大家提交使用bug


1、在 wangeditor 中 onChange 中获取第一个元素（作为wiki页面名称）， 
2、键盘输入的时候会实时更新左侧的Wiki节点名称，如果内容为空，默认显示（未命名）
3、这个元素只能是h1标签，可以修改内容，但不能修改样式，如果修改就return

有一个bug，帮我分析一下 我点击wiki 节点的时候 有一定的几率 要点击对应的节点2次才能切换Wiki页面，如果有延迟输入的话 去掉这个功能，（点击就直接切换） 

上午：设计功能
下午：AI实现
晚上：整理架构